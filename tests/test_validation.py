"""
Placeholder until src/validation.py is implemented (TASKS.md Phase 4).

Must specifically test that the walk-forward split logic never lets a
test-window timestamp precede or overlap its corresponding training
window, and that training rows whose forward target window extends past
the forecast origin are purged (see DECISIONS.md ADR-002).
"""


def test_placeholder():
    assert True
