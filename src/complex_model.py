"""
ML volatility model.

TODO (TASKS.md Phase 3): gradient-boosting or small neural-network model
using engineered features (lagged realized volatility, returns, volume,
etc.). Give it a genuinely fair chance -- reasonable feature set,
reasonable hyperparameter search -- per PROJECT.md section 2. Do not
deliberately weaken it, and do not design it with knowledge of the
baseline's recorded result. Every feature builder must be added to the
look-ahead check in tests/test_target.py.
"""
